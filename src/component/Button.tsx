import send from "../assets/send.svg";

const Button = () => {
    return (
        <button type="submit">
            <img src={send} alt="arrow-button" />
        </button>
    );
};
export default Button;
